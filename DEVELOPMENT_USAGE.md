# 本地开发版使用说明

此分支用于分段素材与独立扩写节点测试，尚未发布到 GitHub 主分支。

## 工作流接法

- 长视频节点的 `segment_material` 接到 **H3 长视频 · 多图提示词扩写** 的 `material`。
- 扩写节点的 `h3_prompt` 接到 H3 条件节点的提示词输入。
- 长视频节点的 `image_1` 至 `image_9` 接到 H3 的参考图输入。顺序生成控制器只提交当前段实际存在的图；没有图片的槽位会从本次任务中移除。
- 原曲、人声、帧数、文件名前缀仍使用原有五个接口。selflift 双采样与 LoRA 设置照常使用。

## 分段素材

音频分析完成后打开审核面板。使用新接口的项目自动启用内置素材；旧项目可通过“启用内置素材管理”切换。

项目默认图片只需上传一次。每段可以选择“使用项目默认”或“本段自定义”，自定义段保持独立。修改默认图不会替换自定义图。每段最多九张，图号按缩略图顺序排列；用途说明需要与图号对应。上传、移除或重排后先保存草稿。

“人物表演”用于唱歌或口播；“空镜 / 环境”用于场景画面。空镜使用环境简报，并取消 H3 人声参考、向 selflift 音频潜空间提供静音；最终合成仍使用原曲。空镜切换会重新生成镜头简报，请重新审核。

## 扩写节点

“API 设置与模型选择”保存本机 OpenAI-compatible 服务地址与密钥，读取模型列表。密钥不进入节点参数、工作流或项目快照。换服务地址而未填写新密钥时，会清除旧服务密钥。

`vision` 将当前段所有图片及说明交给视觉模型；`text` 只发送说明和简报；`manual` 输出手写简报/提示词。视觉模式需要模型支持多图，模型列表存在并不代表具备视觉能力。

“分段扩写预览 / 编辑”选择项目与分段，执行扩写或读取缓存。图像内容、顺序、用途、镜头简报、模式、模型或规则改变会使用新缓存键。节点的 `revision` 可手动增加以重新扩写。续跑时，未完成分段使用当前扩写节点的模型与规则；已经完成的分段不会自动重跑，需要点击重新生成本段。采样器等其他配置继续沿用项目快照。

编辑后保存只影响当前段，并要求在长视频审核面板刷新、确认。没有编辑的成功扩写自动缓存。API 请求失败不会自动重复调用或开始视频生成。

## 验证边界

自动化回归和真实 API/ComfyUI 测试记录见 `DEVELOPMENT.md` 与本地诊断目录。文字格式检查、缓存测试不等于画面、运镜或口型已经验收。开发期间原版插件归档与原工作流均保留。
