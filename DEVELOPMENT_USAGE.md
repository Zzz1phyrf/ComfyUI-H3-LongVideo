# 分段素材与提示词扩写使用说明

本文说明当前分段素材与独立扩写节点的接法；安装与上手步骤见 [README](README.md)，本次更新见 [更新记录](CHANGELOG.md)。

## 工作流接法

- 长视频节点的 `segment_material` 接到 **H3 长视频 · 分镜提示词生成器** 的 `material`。
- 扩写节点的 `h3_prompt` 接到 H3 条件节点的提示词输入。
- 长视频节点的 `image_1` 至 `image_6` 接到 H3 的参考图输入。顺序生成控制器只提交当前段实际存在的图；没有图片的槽位会从本次任务中移除。
- 默认工作流继续使用上述扩写链路。若改用外部工具手写提示词，在审核面板逐段填写“手写提示词”，断开扩写预览到 H3 `prompt` 的连线，改接长视频节点末尾的 `segment_prompt`。该接口原样输出，不扩写、不校验、不回退。
- 原曲、人声、帧数、文件名前缀对应前四个输出；随后是素材包、六路图片、手写提示词和 `fps`，共 13 个输出。将 `fps` 接到 VHS Video Combine 的 `frame_rate`。在自己的 SelfLift 双采样工作流中也应按这些名称接线，并保留已验证的采样与 LoRA 设置；仓库基础示例不包含 SelfLift 双采样。

## 分段素材

音频分析完成后打开审核面板。新项目直接启用内置素材；尚未生成过分段的旧草稿在打开时自动迁移，无需再单独启用。“项目默认参考图”默认收起，展开与收起只影响这个默认素材区的显示，不影响分段控件。已有生成记录的旧项目保留原行为，避免改变旧成片的素材契约。

项目默认图片只需上传一次。每段可以选择“使用项目默认”或“本段自定义”，自定义段保持独立。修改默认图不会替换自定义图。每段最多六张，图号按缩略图顺序排列；用途说明需要与图号对应。所有修改在最后点击“保存并确认”时一次性写入项目。

每段“本段画面”可选择“人物演唱/口播”“人物氛围表演”或“空镜环境”。分析得到的连续前奏、间奏和尾奏会作为无人声依据，使新唱歌项目对应分段默认采用人物氛围表演；这不会自动等同为空镜，用户仍可修改。人物氛围与空镜都取消 H3 人声参考并向 selflift 音频潜空间提供静音，最终合成仍使用原曲。

导演简报按“画面任务、声音关系、开场构图、主体动作、镜头运动、结束构图”六行生成，可逐段编辑。切换“人物演唱/口播、人物氛围表演、空镜环境”时会立即预览对应导演简报；如果简报已手工修改，切换前会请求确认。手写提示词是另一份独立内容，不覆盖导演简报。

## 扩写节点

“API 设置与模型选择”保存本机 OpenAI-compatible 服务地址与密钥，读取模型列表。密钥不进入节点参数、工作流或项目快照。换服务地址而未填写新密钥时，会清除旧服务密钥。

`vision` 将当前段所有图片及说明交给视觉模型；`text` 只发送说明和简报；`manual` 输出手写简报/提示词。视觉模式需要模型支持多图，模型列表存在并不代表具备视觉能力。

“分段扩写预览 / 编辑”选择项目与分段，执行扩写或读取缓存。图像内容、顺序、用途、镜头简报、模式、模型或规则改变会使用新缓存键。节点的 `revision` 可手动增加以重新扩写。仅改变扩写设置时，续跑中的未完成分段使用当前扩写节点的模型与规则；已经完成的分段不会因此自动重跑，需要点击重新生成本段。若采样器等生成工作流参数改变，顺序生成会更新项目快照并将原片段标记为待重生成；参数未变时沿用快照。

编辑后保存只影响当前段，并要求在长视频审核面板刷新、确认。没有编辑的成功扩写自动缓存。API 请求失败不会自动重复调用或开始视频生成。

## 验证边界

自动化回归和真实 API/ComfyUI 测试记录见 [验证记录](VERIFICATION.md)。唱歌分段多图功能已由维护者此前实测确认；本次发布整理不重复生成视频。文字格式检查、缓存测试不等于所有素材的画面、运镜或口型均已验收。口播连续一镜到底实验仍存在景别漂移与果冻感，不作为本次更新承诺。
