# 开发预览版验证记录 · 2026-08-29

## V11 动态参考图本地烟测 · 2026-09-01

- 源码测试：65 项单元与契约测试通过。
- 语法：Python 模块使用当前 ComfyUI 内置 Python 编译通过；`web/h3lv.js` 通过 `node --check`。
- 正式本机节点契约：ComfyUI 0.34.2 返回 15 个输出，包含独立 `h3_prompt` 和 `reference_image_1` 至 `reference_image_9`；已注册 `reference_roles_json`。
- 本地工作流迁移：生成主链已移除 PromptExpand；`h3_prompt` 经过现有文本显示节点直连官方 H3 的 prompt 输入；已有参考图链路顺延到新输出槽位。
- 浏览器烟测：工作流没有出现 UNKNOWN。当前连接两张图片时，节点显示图1、图2和等待连接的图3；参考图职责窗口只列出两张已连接图片，并显示缩略图和职责选择器。
- 本轮未宣称完成：尚未用 V11 重新执行 ASR 分析、H3 采样、口型检查或最终合并视频的视觉验收。

## 安装前隔离测试（历史记录）

- 独立 ComfyUI 测试实例：0.34.0 / 前端 1.49.6，端口 8196；只加载本插件，未加载其他自定义插件。
- 两个节点在真实 `/object_info` 注册成功；音频分析工作流能在 ComfyUI 画布中打开，节点按钮能打开内嵌预览编辑面板。
- 用用户的《迂回 副歌.WAV》完成节点内人声分离、独立环境识别和自动分段，原曲 36.687528 秒；切点 9.86、24.10 秒；原曲样本完整覆盖。
- 实际通过 `H3LVLoadSegment → SaveAudio` 读取第一段，输出 10.125011 秒 FLAC，对应 243 帧 / 24fps，含尾部生成补齐。
- 原曲片段、人声切点试听接口返回有效 WAV；前端原生音频控件和对应地址已检查。浏览器内实际可听播放没有在本轮确认，仍需用户试听。
- API 保存撤销确认、旧版本确认拒绝已实测。测试项目最后保持未确认。
- 12 个自动测试通过，包括连续覆盖、合法帧数、改切点后的提示词重建、顺序队列模拟、未知任务不重复提交、路径隔离。
- FFmpeg 真实合成测试：3 段测试视频裁掉补齐帧后合并成 720 帧、24fps、30 秒，并加入一条完整原曲音轨；这不是 H3 生成测试。
- 前端 JavaScript 语法检查通过。浏览器记录有 ComfyUI 旧菜单弃用、启动初始化及测试服务器重启时的请求失败；没有把这些归因或修复到用户其他插件。
- 两份工作流 JSON 的节点及连线一致性检查通过。生成工作流保留原来的扩写、H3 与采样链，替换手动裁音频入口；副本只保留歌手和舞台两张参考图，原文件未改。

详细真实节点测试结果见 `tests/comfy_smoke_receipt.json`；测试脚本为 `tools/smoke_comfy.py`，不执行 H3。

## 安装前未覆盖范围（历史记录，后续进展见文末）

- 隔离测试当时尚未安装到正式 ComfyUI `custom_nodes`，未变更其 Python 依赖、模型、工作流。
- 隔离测试没有运行 H3，也没有调用已有 PromptExpand 的外部模型服务。真实生成的后续验证单独记录；不能用隔离测试证明口型、段间衔接或成片质量。
- 顺序控制器使用本机当前 ComfyUI 队列接口；换 ComfyUI 版本仍需重新验证。
- 自动分镜只是规则草案，经已有 PromptExpand 节点扩写；不是完整曲式理解，也不保证后一段姿态与前一段尾帧一致。
- 前奏、间奏、尾奏没有经过完整歌曲专门验收。无识别文字标记为人声状态不确定，不自动声称无人声。
- 本歌两个切点仍与估计字时间戳重叠，面板保留警告；不能声称一定没有拆歌词。
- 正式使用前必须在 ComfyUI 里试听确认；当前验收不等于自动剪辑质量合格。

## 正式环境保护

测试目录：`D:\Codex\comfyui工作流\ComfyUI报错相关\_h3lv_comfy_test`。
ComfyUI 核心、Python 与已有模型仅只读复用。没有安装或下载软件；没有修改用户原音频。开发测试服务结束后停止，测试数据保留供复查。

## 正式环境授权试生成 · 2026-08-29

- 用户已授权在开启的 8188 正式 ComfyUI 中进行 H3 长视频测试，使用其现有 PromptExpand 服务、模型及参考图。没有添加新服务、安装依赖或下载模型。
- 旧成片的实际提示词明确要求固定机位；图 2 连接和 H3 输入正确，但成片沿用图 1 的蓝色背景。修复聚焦本插件的通用分镜与项目内 Ref2VA 扩写规则，不改第三方插件或 H3 核心。
- 歌手模式生成具有起止构图的推、拉、横移；相邻高能量段避免连续重复推近；口播模式保留固定机位。图 1 人物与图 2 环境分别映射 Subject，要求结尾保持目标场景。
- 更新后 14 个自动测试通过，涵盖镜头变化、环境绑定和连续推近去重复；这不替代真实视频检查。
- 正式顺序控制器已实际完成首段、按请求暂停，并在继续时跳过已完成首段自动提交下一段。每段沿用用户的真实扩写和 H3 采样链。
- 首段真实画面出现明显拉远，并采用图 2 金色舞台；幅度超过预设。第三、四段初次生成出现下装差异，第四段末尾回到蓝色背景，因此归档原尝试并定向重做。没有删除旧视频。
- 本次项目根据首段补充短裙/高筒靴要求；素材专属服装、金色 LED 等细节没有写入通用分镜代码。上半身人物参考无法唯一约束下装，仍属于实际生成风险。
- 详细视频、真实任务记录、分段扩写提示词与首尾帧抽查保存在相邻 `H3长视频_夜间验收_20260829` 目录。完整合并及音轨校验结果以该目录 `final_validation.json` 与 `检查记录.md` 为准。
- 最终 5 段真实 H3 已完成。第四段只加强提示词仍回退蓝色背景，改用第三段生成画面的统一人物/舞台参考后，首尾抽查保持金色场景；其余四段保留原人物参考。没有把这个单样本对照等同于所有素材的通用保证。
- 真实合并暴露旧时间戳继承造成的累计缩短：1359 帧却只有 56.458333 秒。修复后按帧序号建立 CFR 24fps，并使用 24000 刻度的轨道/容器时基。最终为 1359 帧、864×480、24/1 平均/标称帧率、56.625 秒视频与 56.64 秒原曲音轨。
- 原曲信号对齐：8kHz 单声道比较，相关系数 0.9999983454，估计偏移 0 秒；不作为口型测量结果。合并测试增加非整秒切点、精确视频时长与平均帧率断言，14 项全部通过。
- 本轮没有重启正式服务：分镜由更新后代码准备，再由正式节点/控制器执行；最终时钟修复通过同一插件合并函数重跑合并，不重做 H3。修复代码已部署，服务器下次启动加载。尚未做重启后的完整端到端重跑。

## 分段提示词职责与相邻片段实测 · 2026-08-29

- 实际工作流连线确认：`H3LVLoadSegment.segment_brief` 以可编辑中文简报进入现有 `PromptExpand`，其六字段英文结果再进入 H3；界面简报与最终 Ref2VA 输出语言已经分开。
- 通用“数字人”预设包含独立 15 秒快剪的 Hook/Build/Peak/Resolve 与多镜头规则，不适合作为长视频单段导演。配套 02/03/04 工作流现统一启用项目内单段 Ref2VA 规则，不修改用户全局预设。
- 插件输入已改为结构化单段分镜：整段角色、起止景别、相机运动、舞台绑定、相邻接口、可用音频与补齐时长由插件确定；PromptExpand 只翻译和格式化，不得替换运镜或建立新的段内导演弧线。
- 新规则强制六字段名称及冒号、单一 `[Shot 1]`、图 1 人物/图 2 环境、无虚构歌词、无新增人群声/混响/环境音，原曲只在最终合并阶段恢复。
- 用项目 `bd836b65713b422ab0000e5d3f3c6316` 实际生成相邻第 3、4 段。第 3 段出现真实前推和明显舞台视差；第 4 段从中近景起步并执行横移，人物、服装文字和金色舞台保持可辨。独立生成之间仍是剪辑连续，不是像素级尾首帧延续。
- 首次第 3 段把“中景”执行得接近全身景。加入腰部/胸部硬裁切边界后重做，开场由膝下全身收紧至髋部附近，方向明显改善但仍未完全严格停在腰线；记录为 H3 景别执行偏差，不继续用更多形容词声称已完全解决。
- 19 项自动测试通过，包含所有相邻歌手段开场景别等于前段结束景别、首末段不自动大全景、提示词舞台/音频绑定、帧网格和合并时钟。真实提示词对照和三次 H3 生成另行证明格式、舞台引用和镜头执行，不把自动测试等同于审美验收。

## 新提示词契约完整 5 段实测 · 2026-08-29

- 项目 `bd836b65713b422ab0000e5d3f3c6316` 使用统一人物图、独立舞台图、结构化分镜和专用 Ref2VA 扩写规则，由正式顺序控制器从 0 到 4 逐段生成；五段均成功，随后自动合并完成。
- 最终文件为 `assembly_57171a46/final.mp4`：1359 帧、864×480、H.264、24/1 平均与标称帧率；视频 56.625 秒，AAC 48kHz 双声道音轨 56.640 秒，容器 56.640 秒。
- 五段最终 PromptExpand 输出均包含六字段、单一 `[Shot 1]`、`<Picture 2>` 舞台绑定和硬裁切边界。首段还明确写了 `No additional performers`，但 H3 仍在末尾生成乐队并拉到舞台全景；这是生成执行偏差，不是扩写遗漏。
- 接触表显示：第 1 段末尾全景到第 2 段开头近景跳变明显；第 2 段到第 3 段同时发生景别和舞台变化；第 3 段到第 4 段又回到人物图蓝色背景；第 4 段到第 5 段的金色舞台与中近景衔接相对可用。末段没有重新拉到大全景。
- 完整实测否定了“只靠更详细提示词即可稳定长视频连续性”的假设。下一步若继续，应优先把统一人物/舞台合成参考与上一段实际保留末帧作为下一段的视觉连续性锚点，并保留独立生成之间只能做到剪辑连续、不能保证像素级无缝的产品边界。

## 一体化控制、单段替换与剪辑接口修订 · 2026-08-29

- 源码新增一体化工作流生成器：同一画布包含音频分析和 H3 生成链。ComfyUI 自带运行通过 partial execution 只提交分析输出；分析完成后同步项目编号并自动打开审核。未分析/未确认的独立生成工作流在前端拦截，分段读取节点仍保留后端拒绝作为兜底。
- 控制器新增“停止后续生成”：当前 H3 片段完成后停止提交，区别于强制中断。暂停、停止、继续的状态均保留在项目清单。
- 修改单段提示词只标记该段待重生成；移动一个切点只标记其左右两段。其他完成段及旧成片保持可用，不再由一次保存清空所有 `job`。
- 完成段支持只重生成本段。提交新版本前归档当前成功版本；新版本失败时自动恢复旧成功版本；面板支持恢复上一版、仅重新合成。普通继续沿用冻结工作流快照，明确的单段重生成使用当前画布快照。
- 合并为每个采用版本建立规范化片段缓存键。第二次合成直接复用未变化的 24fps 精确帧片段，只创建新的最终容器并重新封装原曲。
- 实际四段成片边界检查确认合成文件没有漏帧：881 帧、24fps。跳切来自独立生成片段的构图状态不匹配：一处景别偏离计划，另一处人物、麦克风和背景同时换位。新规则在允许两个景别时要求每个边界明确改变景别，并统一人物中心、上三分之一眼线和麦克风屏幕侧；约 30 度机位不再单独承担切镜。运动仍可延续到切点，不强制冻结或停稳。
- `edit_frames` 保留在项目清单和合成器内部，节点不再暴露；生成与合成固定 24fps，节点不再输出冗余 `fps`。已保存工作流把 `filename_prefix` 迁移到新第 5 个输出并将 Video Combine 帧率写死为 24。
- 24 项自动测试通过，新增覆盖：提示词局部失效、移动切点只影响相邻两段、完成段单独替换、替换失败恢复旧版、停止后不提交后续段、规范化缓存复用，以及相邻镜头非同景别同角度。JavaScript 与 Python 语法检查通过。
- 本节是源码与合成状态机验证，没有安装到正式 ComfyUI，也没有再次运行真实 H3；一体化 partial execution、按钮交互及新剪辑提示词仍需安装后重启做现场验证。

## 旧项目审核兼容修订 · 2026-08-29

- 旧项目缺少 `transcript.json` 时，审核接口改用 `segments.json` 中已有的分段文本与时间范围重建只读识别数据，不再向用户暴露底层文件路径。
- 旧项目连分段文本也不存在时，返回“请重新运行音频分析”的中文操作提示；其他项目文件缺失统一返回友好提示。
- 27 项自动测试通过，新增覆盖识别缓存重建、现有缓存优先及无可恢复文本时的错误提示；Python 与 JavaScript 语法检查通过。

## 首次生成状态文案修订 · 2026-08-29

- 首次生成前调整切点仍会重新计算受影响的相邻片段，但不再标记为“待重生成”；只有已经存在成功视频的片段被修改后才进入重生成状态。
- 28 项自动测试通过，新增覆盖首次生成前移动切点只记录受影响片段、不产生旧视频失效标记。

## 一体化节点合并 · 2026-08-29

- 新增 `H3LVUnified`，复用原分析和分段读取契约：原生运行的 partial execution 执行分析；顺序控制器注入已确认项目与索引后输出当前段素材。
- 新生成的 `03_H3长视频_一体化.json` 只包含一个 H3 长视频节点和一个音频上传节点；旧 `H3LVAnalyze` / `H3LVLoadSegment` 继续注册以兼容历史工作流。
- 29 项自动测试通过，新增覆盖合并节点输出契约与控制器类型注册；生成器针对实际数字人工作流验证为 32 个节点、33 条连线。

## 最终成片回填 Video Combine · 2026-08-29

- 外部 FFmpeg 合并完成后，控制器发送 `h3lv-final` 事件，把位于 ComfyUI output 内的 `final.mp4` 描述回填给当前工作流唯一的 `VHS_VideoCombine`，使其预览从最后一个分段切换为最终成片。
- 项目查询同时返回 `final_preview`；错过完成事件或重新打开工作流后，审核面板加载项目时会恢复成片预览。
- 30 项自动测试通过，新增覆盖最终输出路径到 VHS 预览描述的安全映射；Python 与 JavaScript 语法检查通过。

## 结构化导演偏好与整首歌镜头规划 · 2026-08-30

- 删除节点中必填的全片自由文本提示词，改为表演强度、镜头活跃度、最远景别和默认留空的高级补充要求；人物、场景、音频及一致性契约继续由插件内部生成。
- 歌手镜头先按全部分段的相对能量和历史镜头状态规划。首段不再固定拉远，末段没有远景特例，连续横移保持兼容屏幕方向且不会无限重复；低能量段可以采用稳定机位，运动镜头允许在切点仍处于运动中。
- Ref2VA 扩写规则删除“补齐帧前收稳”的统一要求，改为严格执行 `motion_at_entry` / `motion_at_exit`。相对能量只作为结构证据，不被表述为歌词含义或确定情绪。
- 35 项自动测试通过，覆盖新节点输入契约、旧工作流控件顺序兼容、结构化偏好、最远景别限制、运动中剪切、首段不固定拉远、等能量段的景别去重复、相邻剪辑关系、运行时采用插件当前六字段扩写规则、原有分段/替换/合并状态机。Python 与前端脚本语法验证通过；真实 H3 审美效果仍需用户重新分析并生成后验收。

## 多图片角色清单与可选AI导演 · 2026-08-30

- 三个参考图接口改为四种明确图片组合，不再用“有几张图”猜角色。单图复合、双人图1含场景、单人+独立场景、双人+独立场景分别生成稳定的 performer/environment Subject 和 Picture 映射；旧项目默认保持图1人物、图2场景。
- 双人模式保存人物1主唱、人物2主唱或两人合唱。场景没有 speaker ID，未分配演唱的人物被明确禁止口型跟随。
- 规则导演继续离线工作。可选AI导演通过用户目录设置调用 OpenAI兼容接口；仅发送实际使用的参考图、分段测量和未校对识别文字，不发送音频。Key不返回前端、不写工作流和项目文件。远程HTTP被拒绝，只允许HTTPS或本机HTTP。
- AI镜头表只接受限定景别、前侧角度、五种运镜和三档表演强度；数量、索引、推拉起止关系及相邻剪辑不合法时直接失败，不静默退回规则模式。
- 40 项自动测试全部通过，包含四种角色映射、双人主唱分配、AI镜头扩展/拒绝、Key隔离、旧状态机、真实FFmpeg合成；Python编译与前端JavaScript语法检查通过。
- 隔离 ComfyUI 0.34.0 实际加载新节点并完成规则模式音频分析、三段生成计划、音频预览、确认、分段读取和失效逻辑；未调用AI服务、未运行H3。回执位于隔离测试目录 `smoke_v4.json`。

## 单歌手图片模式与节点内AI设置入口 · 2026-08-30

- 第一版范围收敛为单歌手：单图由图1同时提供人物与环境，双图由图1提供人物、图2提供独立环境。新节点不再提供双人组合或第三张参考图。
- “演唱分配”保留为隐藏的单值兼容槽，仅避免已有工作流后续控件错位，不显示、不参与导演或提示词逻辑。
- 分析与一体化节点直接显示 AI API 已配置/未配置状态和设置按钮；选择 AI 导演但尚未配置 Key 时，在分析提交前打开设置入口。
- 40 项自动测试全部通过；前端 JavaScript 语法检查通过。未调用真实 AI 服务，节点按钮和状态仍需重启正式 ComfyUI 后人工确认。

## 中文分段简报、参考图合成与真实四段验收 · 2026-08-30

- 分段审核区改为可读中文导演简报；它仍是可编辑的规划输入，不冒充最终 H3 提示词。PromptExpand 继续把简报整理为六字段英文 Ref2VA 提示词，控制器会保存实际执行版本供追溯。
- 节点输出收敛为原曲、纯人声、分段简报、生成帧数和文件名前缀；`edit_frames` 只在项目和合成器内部使用，整条 H3 生成/裁切/合并链继续固定 24fps。已保存的一体化工作流同步迁移。
- 旧实测项目 `e48a6bf20c794c7c9484ba50c2376513` 的逐段接触表暴露了真正的数据流错误：BiRefNet 的人物图输出仍带原始暗色背景，而 mask 输出完全未接；H3 同时收到暗色人物图和金色舞台图，运镜时会在两种背景之间漂移。
- 一体化工作流新增 `ImageCompositeMasked`：以独立舞台为 destination、人物原图为 source、BiRefNet mask 为遮罩，合成后的统一人物舞台图再进入预览、H3 和导演参考。预览实测确认人物已从暗色背景抠出并置入金色舞台。
- 修正后的真实项目 `7828111208714ed38cb9dfecbdb7f841` 完成四段顺序生成和自动合并。最终 `final.mp4` 为 864×480、881 帧、24fps、36.708333 秒，含视频和原曲音轨；四段任务均 completed。
- 新接触表确认暗色/金色背景漂移已经消失，人物、服装、麦克风和金色舞台保持可辨；第 2 段有明显前推，第 3 段实际产生拉远变化。但 H3 仍未严格服从中景/中近景裁切边界，第 2、3 段出现全身远景，导致硬切尺度仍然较大。这证明剩余突跳是独立生成首尾构图执行偏差，不是合并漏帧，也不能再靠增加同义提示词宣称解决。
- 40 项自动测试通过；最终提示词抽查确认中景到中近景、构图锚点和舞台约束均已写入，模型执行偏差与提示词遗漏已分开记录。

## 单张合成参考 A/B 实测 · 2026-08-30

- 从双图项目 `7828111208714ed38cb9dfecbdb7f841` 建立隔离对照项目 `f749fa14f3f145b495708aac7d648084`。两者使用完全相同的音频切点、AI 镜头表和生成参数；只重做第 2、3 段。
- 对照版从 H3 输入中移除独立舞台图，只保留人物已经抠图合成到舞台后的单张参考。项目清单同步改为 `single_composite`，最终简报不再引用 Picture 2；原双图项目及成片未被覆盖。
- 第 2 段从“双图版全身远景起步再推近”改善为“中景起步后连续推近”，说明独立大全景参考会增加 H3 擅自拉远的概率。
- 第 3 段出现相反结果：单图版前半段把金色室内舞台改造成户外城市演唱会，随后才逐渐恢复金色舞台；双图版虽然景别执行错误，但环境始终受独立舞台图约束。
- 结论：单图减少构图竞争，但缺少完整环境锚点；双图增强环境稳定性，却给模型更大的重构景别空间。单图不能直接作为所有图片模式的默认替代，后续需把“人物构图锚点”和“环境锚点”分开控制。
- 两组实际 PromptExpand 输出都把规则要求的 `<Picture 1>` / `<Picture 2>` 写成了无尖括号的 `Picture 1` / `Picture 2`。这个格式偏差同时存在于 A/B 两侧，不影响本次相对比较，但可能削弱 H3 的正式参考绑定，应作为独立问题修复和复测。
- 单图对照最终成片仍为 864×480、881 帧、24fps、36.708333 秒，包含原曲音轨。逐段接触表和 A/B 四帧比较保存在 `transition_diagnostics/single_reference_ab/f749fa14f3f145b495708aac7d648084`。

## 角色四视图＋独立场景 AI 导演实测 · 2026-08-30

- 使用内置生图工具重新制作两张测试素材：图 1 是同一成年男歌手的正面、左右侧面和背面四视图，图 2 是无人物的金色室内舞台。为避免把四个视图抠图后同时贴进舞台，本轮绕过人物/舞台合成节点，直接把原始四视图和独立场景分别接入 H3 的两路参考图。
- 项目 `d2cd80e5918f41bd9f6b0fd283593e89` 由已配置的 AI 导演重新分析，生成四段镜头表：中景推到中近景、固定中景、中近景拉到中景、中近景向右横移；随后由正式顺序控制器完成四段生成和自动合并。
- 最终 `final.mp4` 为 864×480、881 帧、24fps、36.708333 秒，包含视频与原曲音轨。四段均只出现一名歌手，没有把四视图误解为四名人物；人物身份、白色服装、黑色麦克风和金色舞台全程保持可辨，没有再次出现户外场景或暗色背景漂移。
- 运镜方向只有部分执行：第 1 段确实前推，第 2 段基本固定，第 3 段确实拉远，第 4 段存在横移构图；但实际景别幅度没有服从计划。第 1 段从中全景推近，第 2 段保持中全景，第 3 段从脸部近景一路拉到全身远景，均越过了“最远中景”的约束。
- 三个相邻边界仍然不合格：第 1 段近景结尾硬切到第 2 段中全景；第 2 段结尾人物放下麦克风后硬切到第 3 段脸部近景并重新举麦；第 3 段全身远景结尾硬切到第 4 段中近景。合并文件没有漏帧，跳变来自每段独立生成的实际首尾状态未受上一段画面约束。
- 本轮证明四视图能改善单人身份稳定性，独立场景图能改善环境稳定性，但不能替代跨段视觉状态约束。AI 导演目前只规划抽象景别和运镜，无法在生成前知道 H3 对上一段终态的实际偏差，因此不能单靠重新写分段提示词保证剪辑连续。
- 四段实际 PromptExpand 输出仍把规则要求的 `<Picture 1>` / `<Picture 2>` 改写为无尖括号的 `Picture 1` / `Picture 2`；与前次 A/B 一致，保留为独立格式问题，未在本轮中途修改变量。

## 长歌剪辑连续与固定机位口播完整实测 · 2026-08-30

- 演唱项目 `2d2f9ef7dbf741f6a623ebcb9f38f5c7` 使用 58.282086 秒音频、单人角色设定表和独立海边场景，由 AI 导演规划五段。实际生成暴露两个模型执行问题：中景会受设定表中小型全身图误导，长时间后拉/中景横移会越界到全身景。
- 通用修订把设定表多视图限定为身份和服饰证据，禁止复制表格、生成多人或由小全身图决定景别；自动/AI 镜头取消后拉，并把横移限定在中近景。只替换问题分段，前面通过的生成片段和旧 take 都保留。
- 演唱最终文件为 `assembly_5798e859/final.mp4`：H.264 864×480、24fps，AAC 44.1kHz 双声道，58.291667 秒，12,127,492 字节。四个切点均是规划的景别+机位差异，末段 12 秒保持正面中景固定机位，没有再自行拉到全身。这是可剪辑的硬切连续，不是像素级尾首帧无缝。
- 口播项目 `6c97f4af8a6a4f12b93573e22a21dfe7` 使用 43.583855 秒音频和单张 9:16 人物+场景图，四段全部由本地规则导演生成相同正面中近景固定机位简报；未调用插件 AI 导演，未向导演 API 上传参考图。
- 口播首次实测机位稳定，但第 1 段旗袍由蓝色渐变为紫色，第 2 段又从参考图的蓝色开始。这证明是段内色相漂移，不是参考图未连接或合并漏帧。固定机位规则因此新增通用外观锁定：逐帧保持参考图的服装主色、色相、纹样、材质、表面光泽与肤色，禁止重染和渐变色偏。
- 新规则重新生成后，四段内部接触表和三个切点接触表均保持蓝色旗袍、一致人物尺度、头部位置、桌面道具和背景透视。口播最终文件为 `assembly_17fc2957/final.mp4`：480×864、24fps、1046 帧，43.583832 秒，含原音频音轨。
- 正式 ComfyUI 已在部署新规则后重启并完成端到端实测。47 项自动测试全部通过，Python 编译和前端 JavaScript 语法检查通过。实图可以证明本轮两组素材的改善，不应外推为所有角色、场景和音频都保证同等执行精度。

## 单图口播逐段换衣修复复测 · 2026-08-31

- 对照源图 `口播.png` 为深蓝色银花旗袍。普通数字人 FL2V 任务保持深蓝；修复前长视频项目 `2bf38346c9c04758a920a27eaf5c65f3` 的第 1、2 段分别生成米白花衣和青绿色花衣，确认不是轻微色偏或预览缓存。
- 后端历史确认两边都使用 `MiniMaxH3ReferenceToVideo`、同一 `minimax_h3_fl2va_pruned_int8_convrot.safetensors`、同一 Turbo LoRA 和同一参考图。长视频独有差异是实验性 `MiniMaxH3MemoryEfficientSageAttentionPatch`、从 `SamplerCustomAdvanced.denoised_output` 解码，以及单图简报仍把同一图片拆成两个 Subject。
- 工作流保留 Ref2V 音频参考节点，改回普通数字人已验证的 `ModelAttentionBackend / comfy kitchen attention`，VAE 解码改接采样器 `output`，单图简报与最终规则只定义 `<Subject 1>` 和独立一行 `<Audio 1>`；口播正文限制为一个 `[Shot 1]`。
- 从原项目克隆隔离项目 `2addd767d7ef45d09e9485d399cd3f65`，只重新生成前两段。两段的开头、中间、结尾全部保持深蓝旗袍、银色纹样、固定正面中近景和同一背景；第 1 段结尾与第 2 段开头的构图尺度一致。旧项目及旧视频未覆盖。
- 按各段 `edit_frames` 裁切并配回连续原音频的两段预览为 `renders/two_segment_assembled_preview.mp4`，21.290998 秒。PromptExpand 独立复测确认单图输出没有 `<Subject 2>`，`<Audio 1>` 单独定义并绑定 `<Subject 1> (S1)`，正文只有一个 `[Shot 1]`。
- 52 项自动测试通过。由于 Attention、采样输出和单图语义在同一修复批次内对齐，本轮证明的是长视频执行路径组合导致服装重构，不把结果伪装成对 Sage Attention 单一变量的因果证明；若要精确量化三者贡献，需要额外逐项真实渲染 A/B。

## V1 镜头合同与 GitHub 朋友测试候选 · 2026-08-31

- 节点创意控制收敛为“镜头活跃度”和“最远允许景别”；旧表演强度与自由文本导演说明仅保留为隐藏兼容槽。ASR Python、模型目录和设备移入可折叠技术设置。
- AI 导演请求改成通用的独立参考图视频镜头规划，不假设远端模型认识 H3，也不再传递历史测试结论或模棱两可的“证据”措辞。AI只返回景别、角度和段内运镜；插件本地校验 auto 至少一半、dynamic 至少三分之二片段有真实运镜，并拒绝连续两段固定机位。
- 分段输入固定为 `H3LV_SEGMENT_V1` 中文合同，逐项声明图片角色、开场构图、段内运镜、结束构图、入口剪辑、出口运动状态、表演、手持道具与穿戴配饰。PromptExpand 随附规则逐字段映射到六部分 Ref2VA 提示词，只生成一个 `[Shot 1]`，不发明首饰、手表、戒指、麦克风或第二张参考图。
- 节点设置入口统一改名为“导演模型 API Key”；设置说明明确该 Key 只保存在当前 ComfyUI 用户目录，不写入工作流、项目文件或日志，并与 Prompt Assistant 自己的大模型配置相互独立。
- 56 项源代码自动测试通过，新增验证发送给导演模型的请求不包含 H3 名称、`vocal_state` 或含糊的“不确定证据”措辞。Python 编译与前端 JavaScript 语法检查通过。正式安装目录的 12 个部署文件与源文件 SHA-256 一致，当前工作流的 PromptExpand 自定义规则与插件随附规则一致。
- 公开候选从空目录按白名单构建，包含 21 个节点、32 条连线的脱敏示例工作流；清除了项目 ID、ASR 绝对路径、素材文件名和旧视频预览路径，移除了 ShowText 与 rgthree 可选节点。机器耦合扫描无发现，发布审计只报告 9 个可分发源码文件。
- 本轮没有启动 ComfyUI，也没有真实调用导演 API、PromptExpand 或 H3 渲染。跨机器安装、最终 H3 执行和审美连续性必须由朋友测试继续验证；许可证仍需仓库所有者在公开发布前选择。

## 单一公开节点与输出目录审计 · 2026-08-31

- 删除 `H3LVAnalyze` 与 `H3LVLoadSegment` 的节点注册、前端分支、控制器识别、公开文档和旧工作流生成/冒烟工具。插件只注册 `H3LVUnified`；分析器和分段读取器类仍作为一体化节点的内部实现复用，没有为删除两个菜单入口进行无关重构。
- 56 项自动测试通过，新增断言公开节点映射恰好只有 `H3LVUnified`；前端 JavaScript 和 Python 语法检查通过。源码、实际安装目录和 GitHub 候选同步更新。
- 只读审计当前 `output/H3LongVideo`：61 个随机 ID 项目、934 个文件、合计 2.795 GB。WAV 约 1.78 GB，`renders` 约 0.54 GB，`assembly_*` 约 0.35 GB，`normalized` 约 0.11 GB。完成项目同时暴露原曲/人声、诊断 JSON、原始 take、标准化缓存和一个或多个合成目录，用户无法快速定位最终成片。
- 本轮没有移动或删除任何已有输出。建议下一版把用户可见成片与内部项目工作区分层，并为历史项目提供兼容读取和显式清理策略；这属于单独的数据迁移，需确认后实施。

## 成片/项目分层与旧测试数据清理 · 2026-08-31

- 新项目根改为 `output/H3LongVideo/projects/<project_id>`，内部固定使用 `state`、`audio`、`takes`、`cache`、`work`。最终 MP4 独立写入 `output/H3LongVideo/final_videos`，名称为创建时间、模式和项目短编号；同一项目重新合成时原子替换同一路径。
- 分段 VHS 前缀改为 `H3LongVideo/projects/<project_id>/takes/seg_XXXX`。计划、分析、识别和队列快照进入 `state`；原曲和人声进入 `audio`；标准化片段进入 `cache`。成功合并后删除对应的 `work/assembly_*`，失败尝试可保留用于诊断。
- 审核项目下拉增加“唱歌/口播”标签，并增加“复制成片目录”按钮。当前保存工作流的旧项目 ID 和 VHS 历史预览已清空，不改变模型、LoRA、采样或参考图连线。
- 57 项自动测试通过。真实 FFmpeg 合成测试确认 24fps、720 帧、30 秒音视频、第二次合成复用 cache、最终路径稳定且成功后 work 为空；Python 与 JavaScript 语法检查通过。
- 清理前再次确认 ComfyUI 未监听 8188，旧根目录中恰有 61 个 32 位十六进制项目目录、934 个文件、2.795 GB，且没有任何新结构项目。用户明确授权后，这 61 个旧开发测试目录被逐个送入 Windows 回收站；根目录、模型、工作流、输入素材和用户目录中的 API 设置未删除。
- 清理后旧项目数为 0；新的 `projects` 与 `final_videos` 目录存在且为空。正式安装目录 12 个部署文件与源码哈希一致，GitHub 候选 14 个发布文件与源码哈希一致；候选机器耦合扫描无发现，发布审计只报告可分发源码。
- 本轮没有启动正式 ComfyUI、调用导演 API、PromptExpand 或 H3。首次新项目仍需启动后验证目录创建、分段 take 写入、面板复制路径和真实最终成片预览。

## 干净安装与四套素材真实局部验收 · 2026-08-31

- 把当前已安装插件与 `ComfyUI/user/H3LongVideo` 私有设置整体移出后，从 GitHub 候选目录重新复制安装。首次启动只注册 `H3LVUnified`，旧双节点对象为空；导演 API 未配置，候选目录不含设置、Key、缓存或输出。旧安装和私有设置保存在独立可恢复备份中。
- 恢复私有用户设置后，使用当前工作流的 `FL2VA int8 + turbo_v4 LoRA 1.0 + 10 steps + comfy kitchen + 0.4MP` 生成 9 个真实 H3 片段：两套横屏唱歌共 5 段，两套竖屏口播共 4 段。横屏为 864×480，竖屏为 480×864，全部 24fps 且含音频流。
- 第一首唱歌素材的 `source.wav` 与 `vocals.wav` 不同：单声道相关系数约 0.689，原曲 RMS 约 0.275，人声 RMS 约 0.173，证明未连接 vocals 时的本地 HDEMUCS 分离实际执行。
- 唱歌 A 第 1 段横移可见，第 2 段固定；唱歌 B 前两段因低能量规则均固定，第 3 段高能量横移可见。两组人物和服装总体稳定，景别+机位硬切比同轴缩放更明确，但独立生成仍改变部分背景构图。
- 两套口播均保持参考服装颜色和原有配饰，没有新增项链、手表或戒指。口播 A 段间构图接近；口播 B 即使两段最终提示词都要求同一固定中近景，第 2 段仍明显更紧，证明独立任务重构图不能靠增加同义固定机位词彻底消除。
- 当前 `qwen3.8-max` AI 导演多次产生不满足本地约束的镜头表：先只改变景别或机位之一，实验性反馈重试又左右前侧跨轴。未保留自动重试，避免隐藏的二次 API 费用；候选 README 明确说明失败会报错、不自动重复调用或静默降级。
- 本轮只跑每套前 2–3 段，没有完成任一整首项目。因此自动最终合并、`final_videos` 写入和合并节点最终成片预览未在本轮真实素材上验证；四个局部裁切拼接预览仅用于人工检查，不冒充正式成片。
# V12 camera-planner boundary — 2026-09-01

- Confirmed `H3LVUnified` registers with no image inputs, no image outputs and no `h3_prompt` output. Its five outputs are original audio, vocal reference audio, `segment_brief`, generation frames and filename prefix.
- Migrated the supported local workflow so LoadImage nodes connect directly to `MiniMaxH3ReferenceToVideo`; `segment_brief` connects to PromptExpand; PromptExpand connects through ShowText to the H3 prompt input.
- `H3LV_CAMERA_V1` contains only timing, composition, camera motion, entry continuity, exit motion and performance rhythm. Tests reject `<Picture N>` or `<Subject N>` claims inside this camera brief.
- Sequential generation validates exactly one PromptExpand, the `segment_brief -> source_text` connection and a non-empty project material description.
- The AI director request contains no reference manifest, image role, image count, image bytes or audio bytes.
- Verification passed: 65 unit/workflow tests, Python compile checks, JavaScript syntax check, 30-node/37-link bidirectional workflow check, live ComfyUI registration on temporary port 8199, and live object-info inspection of the node contract.
- No H3 model render was run. Camera quality, prompt-assistant expansion quality and lip synchronization still require the user's next real generation test.

# V13 本地双规则与两阶段运行 — 2026-09-01

- 删除插件的 AI 导演实现、导演 API 设置入口和自由文本导演规则；Prompt Assistant 自己的大模型配置保持独立，不受此次修改影响。旧工作流中的 `director_mode` 只保留为隐藏兼容槽，后端不再读取它。
- 唱歌模式由本地配置按音频能量与音频哈希种子安排推、拉、摇、移和轻微重构；每段必须有运镜，相邻段不能重复同一运镜族，左右向运镜再次出现时优先反向。口播模式固定为连续的正面中近景稳定机位。
- 原生 ComfyUI 运行改成两阶段状态机：没有已分析项目时只执行到一体化节点并打开分段审核；项目保存确认后，关闭审核界面再次运行整张工作流，才启动按段顺序生成与最终合成。单独运行 H3 长视频节点始终用于重新分析并创建新项目；审核界面的“开始顺序生成”仍保留为快捷入口，“重新分析音频”不会删除旧项目。
- 59 项单元与工作流静态测试全部通过；Python 模块编译和前端 JavaScript 语法检查通过。测试覆盖本地规则可复现、唱歌相邻运镜族不重复、横向运镜方向交替、口播固定机位、旧 AI 项目字段被忽略，以及两阶段前端入口存在。
- 本轮验证没有调用 H3 模型，也没有生成付费视频。真实 ComfyUI 按钮交互、整段顺序循环、画面运镜执行度和段间视觉衔接仍需在本机启动后由真实任务验收。

## V13 当前模式规则编辑器补充 · 2026-09-01

- 规则入口跟随节点 `mode`：`singing` 只显示唱歌规则，`speaking` 只显示口播规则。保存和恢复默认都只修改当前模式，另一套规则保持不变。
- 后端仍以同一个 schema 2 文件原子保存两套规则，避免拆成多个设置来源；前端只抽取当前模式子对象供编辑，保存时合回完整配置并执行统一校验。

## V13 精简分段镜头简报 · 2026-09-01

- 从交给提示词小助手和用户编辑的简报中移除“协议、片段编号、模式”三项内部元数据，只保留生成时长、开场构图、段内运镜、结束构图、入口衔接、出口运动状态和表演节奏。
- 项目段号、唱歌/口播模式和规则版本继续保存在项目状态中；旧的 `H3LV_CAMERA_V1` 项目仍可读取和生成，新分析项目使用精简格式。
- 提示词小助手随附规则同步改为只解析七个可执行字段，避免把内部元数据扩写成画面内容。

## V13 可选素材说明 · 2026-09-01

- 删除前端与顺序控制器对提示词小助手“用户提示词”的必填校验；素材说明留空不再阻止顺序生成，`segment_brief -> source_text` 连线仍然必检。
- 素材说明留空时，提示词小助手只根据镜头简报生成六段式提示词，不分配图片角色、不创建编号 Picture/Subject/Audio 关系，也不补写身份、服装、环境、道具或配饰。用户填写时，声明内容仍是这些素材关系的唯一依据。
## V13 分段列表降噪 · 2026-09-01

- 从分段卡片标题移除高/中/低置信、端点和剪辑类型标签；这些诊断值继续保存在项目中，并继续用于时间轴切点颜色和内部规划，不占用普通审核界面。
- 待重生成与真实任务状态标签继续保留，因为它们直接影响用户操作。

## V13 顺序生成无干扰启动 · 2026-09-01

- 已确认项目从 ComfyUI 运行按钮启动顺序生成后不再自动打开分段审核界面，只显示短暂的右上角启动提示；首次音频分析完成后仍会自动打开审核界面。
- 增加同一项目的前端启动锁。连续点击时只提示任务正在启动或运行，不会重复提交；后端原有项目任务锁继续作为第二道保护。

## V13 失败重试提示澄清 · 2026-09-01

- 失败段再次运行时，弹窗明确说明这是“上一次没有生成完成”，展示项目中记录的失败原因，并说明成功后会继续后续分段。
- 确认按钮显示实际段号，例如“重跑第 1 段并继续”，避免被误解为新的分段确认流程。

## V13 节点重分析与整图生成分流 · 2026-09-01

- 兼容 ComfyUI 当前“运行所选节点”的执行路径参数：即使前端提交的是从所选 H3 节点推导出的下游输出节点，仍通过画布选择状态识别为 H3 单节点运行。
- 选中 H3 后点击节点浮动工具栏的运行按钮始终重新分析并创建新分段项目；右上角整张工作流运行仅在现有项目已确认后启动顺序生成。

## V13 显式重新分段入口 · 2026-09-01

- 节点增加“重新分析并分段”按钮；审核控制中的对应按钮同步改名。触发后按当前节点参数创建新项目，旧项目和文件不删除。
- 失败段重试弹窗增加“重新分析分段”选项；右上角整图运行继续表达生成或重试，不再兼任重新分析意图。

## V14 两项式分段镜头简报 · 2026-09-01

- 新分析项目的可编辑简报只保留“镜头方案”和“表演节奏”；生成时长继续由 `generation_frames` 控制，不写入提示词。
- 开场构图、唯一运镜、结束构图和结束运动状态合并成一条自洽的镜头方案；入口剪辑信息仅留在项目结构中。
- 环绕运镜明确为摄影机实体移动，不是原地摇镜或水平横移；规则允许时自动转到相容的结束侧角，避免“环绕但始终正面”的冲突。
- 旧七字段项目仍可校验、重试和生成，打开时不自动改写；点击“重新分析并分段”后创建两项式新项目。
