# MiniMax H3 长视频节点开发交接

更新时间：2026-09-20

## 当前工作区

- 仓库：`D:\Codex\comfyui工作流\MiniMaxH3-LongVideo\development\ComfyUI-H3-LongVideo`
- 分支：`feature/segment-materials-expander`
- 安装目录：`D:\ComfyUI\ComfyUI-py312\ComfyUI\custom_nodes\ComfyUI-H3-LongVideo`
- 当前测试工作流：`D:\ComfyUI\ComfyUI-py312\ComfyUI\user\default\workflows\MiniMaxH3无限时长数字人工作流 By 像素幻想Lab (self lift双采)_开发测试.json`
- 远端：`https://github.com/Zzz1phyrf/ComfyUI-H3-LongVideo.git`
- 当前分支尚未推送 GitHub。

## 用户要实现的产品形态

长音频分析切分后，在审核面板为项目设置最多六张默认参考图，也可为每个分段选择“使用项目默认”或上传最多六张本段自定义图。长视频节点输出当前分段的原曲、人声、生成帧数、文件名前缀、`segment_material`、六路动态图像、手写提示词和 24fps；独立的 `H3LVPromptExpand` 节点读取素材包，用多图视觉模型扩写 H3 提示词。口播保持固定构图，唱歌根据本地运镜规则安排景别和运动。

## 本轮已经完成

- 长视频节点在现有 12 个输出末尾追加 `fps: FLOAT`，固定输出 `24.0`；现有输出编号均不变。开发测试工作流已将该接口连到 VHS Video Combine 的 `frame_rate`。
- 顺序生成开始时现在会比较当前生成图与上次快照；分辨率、采样器、模型或提示词链发生变化时，保留原音频分析项目、切点和手工修改，但将已完成分段标记为“生成工作流参数已修改”并按当前画布重新采样。工作流未变时仍复用旧分段。
- 修复分镜提示词生成器对单镜头标签格式的过严失败：当六段结构完整、`detailed_description` 唯一且完全没有镜头标签时，校验器会在该段开头自动补一个 `[Shot 1]`；当视觉模型把同一个标签写进其他章节，或返回 `[SHOT1]`、`[ Shot 1 ]`、`[SHOT #1]`、Markdown 加粗及标签位置偏移等格式变体时，会统一归一为详细描述区唯一的 `[Shot 1]`。已经规范的旧提示词保持原文不变；`detailed_description` 内重复、出现任意形式的 `[Shot 2]`、错误编号或无法识别的畸形标签仍会拒绝。扩写系统规则同时明确标签只能写在详细描述区，并禁止引用、转录、翻译或改写参考图中的海报、标牌、标签和界面文字。
- 新增保守的 ASR 幻觉隔离：按 Faster-Whisper 词级时间戳异常分数检测，并要求异常文本前后都存在至少 2 秒无识别文字区才标记。原始文字、模型概率和时间戳仍保留在 `transcript.json`，但疑似幻觉不参与歌词切点和人声状态判断；审核界面会以橙色显示“疑似 ASR 识别幻觉，已忽略”。没有使用固定短语黑名单，也没有启用可能误删歌曲长音的激进 VAD。现有旧项目不会被静默改写，重新分析后才应用。
- 保留素材说明旁的“@ 引用图片”按钮作为可发现的缩略图选择入口；无图或继承默认素材而不可编辑时，按钮现在使用明确的禁用光标和原因提示，不再误显示为正在加载的转圈状态。
- “复制默认图到本段”只在当前使用项目默认且默认图非空时可用；切换为“本段自定义”后会禁用并提示当前已经是自定义状态，避免误覆盖已有的本段图片和素材说明。
- 分段确认与生成素材检查已拆开：保存并确认只确认切点、画面类型、导演简报和素材配置，不再因尚未上传参考图而失败。真正开始生成前，控制器会一次列出所有待生成但缺少有效参考图的分段，并在写入队列快照或修改运行状态之前停止提交。
- 审核界面存在未保存修改时点击关闭，现在提供“保存并确认后关闭 / 不保存并关闭 / 取消”三种选择；选择保存后关闭只出现这一层确认，不会再被缺图校验阻断。
- 审核面板的分析项目下拉框右侧新增“管理项目”。管理弹窗支持按草稿/完成/失败筛选、多选删除和“全选当前筛选”（部分选中时显示半选状态），并显示每个项目的状态、项目缓存占用及关联成片占用；当前项目有明确标识，正在生成或合成的项目禁止删除。
- 删除默认只清理项目目录（分析音频、切分状态、扩写缓存、参考图和分段视频）以及 ComfyUI 输入目录中的参考图镜像，最终合成视频保留。只有用户额外勾选“同时删除最终合成视频”并再次确认，才会删除位于插件最终成片目录内的关联文件；目录外文件不会被删除。
- 分段素材说明增加图片提及：输入 `@` 或点击“@ 引用图片”会显示本段当前参考图缩略图，选择后插入 `@图N`。图片重排时提及按文件身份自动改号；仍被素材说明提及的图片不可直接移除。项目默认素材说明和本段自定义素材说明共用同一套交互。
- `@图N` 只作为便于编辑的界面语法保存；扩写请求发送给模型前会转换为 H3 Ref2VA 的 `<Picture N>`，越界图号在调用 API 前拒绝。
- 修复“生成提示词（自动复用缓存）”在素材说明留空时读不到缓存的问题：旧 `packet()` 校验在计算缓存键之前错误要求素材说明必填，现在只要求存在参考图。接口会返回 `saved / cache / api / manual` 来源，前端明确显示实际读取来源。
- 旧缓存只在模型、扩写规则、分段上下文、API 地址和内置 H3 规则全部相同时复用。项目 `023021cb` 的 8 份缓存生成于旧版内置规则，与当前键不匹配，因此不会伪装成当前有效缓存；首次点击会重新调用 API，随后才能精确复用。
- `H3LVPromptExpand` 显示名改为 **H3 长视频 · 分镜提示词生成器**，开发测试工作流的自定义节点标题也已同步。节点大文本框标注为“扩写规则（指导模型如何反推 H3 提示词）”，内部字段仍叫 `rule`，旧工作流兼容。
- 分段的“本段画面”三种状态改为动态导演简报预览：下拉切换后立即使用同一套后端导演规则重写当前段简报，不提前写入项目；已手工编辑的简报在切换前会提示覆盖风险。
- 删除审核面板中可见的“保存草稿”按钮；“保存并确认”仍会先保存所有切点、参考图、素材说明、画面类型、导演简报和手写提示词，再确认项目。关闭含未保存修改的面板时会要求确认放弃。
- 修复新空白画布下的素材界面状态不一致：新分析项目现在默认启用内置素材，未生成过的旧草稿打开时也会自动迁移，不再依赖长视频节点的图片输出是否已接线。
- 项目默认参考图区保留默认收起的展开/收起功能；折叠现在只影响这个默认素材区的显示，分段的“参考图来源 / 本段画面 / 手写提示词”始终独立可用。
- 保留的旧版分段素材界面在画布没有 `ref_image` 槽位时会直接禁用上传按钮，不再允许点击后弹出“操作失败”。
- `7c1241f`：新增音频阶段感知的导演简报、人物氛围表演模式、扩写规则上下文和手写提示词直通输出。
- 新增音频语义映射：长前奏可覆盖连续多段，新唱歌项目对疑似前奏/间奏/尾奏默认使用“人物氛围表演”，而不是固定人物演唱；用户仍可切换为人物演唱或空镜。
- 镜头简报改为“画面任务 / 声音关系 / 开场构图 / 主体动作 / 镜头运动 / 结束构图”六行导演简报，并保留逐段编辑。
- 扩写素材包和系统规则增加音频阶段、画面类型、生成帧数与 24fps 生成时长；人物演唱/口播才使用人声参考，人物氛围与空镜均不要求口型。
- 长视频节点在 `image_6` 后追加 `segment_prompt`。审核面板新增逐段手写提示词输入，接口原样输出，不扩写、不校验、不回退；默认工作流连线仍是 `H3LVPromptExpand → PreviewAny → H3 prompt`。

- `cd3ea75`：增加每段素材与独立多图提示词扩写节点。
- `10c2f0c`：参考图与输出口收敛为六张；分段试听改为原曲；审核面板精简。
- `991c70b`：删除节点上的“镜头活跃度”和“最远允许景别”，景别及运镜统一由本地运镜规则控制。
- `abf363e`：删除废弃的 `segment_brief` 输出，当前节点共 11 个输出；旧队列快照自动迁移输出编号。
- `6a2c7d9`：项目默认参考图区改为默认折叠、可反复展开/收起；分段继承项目默认图时，上传按钮使用明确禁用光标，不再显示等待转圈。

运行时代码、前端与测试均已复制到安装目录。按用户要求没有代替用户重启 ComfyUI；下次启动后才会加载本轮节点输出与前端变化。

## 当前输出编号

| 编号 | 输出 |
|---:|---|
| 0 | `original_audio_padded` |
| 1 | `vocals_padded` |
| 2 | `generation_frames` |
| 3 | `filename_prefix` |
| 4 | `segment_material` |
| 5–10 | `image_1` 至 `image_6` |
| 11 | `segment_prompt`（手写提示词原样输出，默认不连接） |
| 12 | `fps`（固定 `24.0`，连接 VHS Video Combine 的 `frame_rate`） |

控制器的旧快照迁移版本为 `output_contract_version = 4`。不要把已删除的 `segment_brief`、`camera_activity` 或 `widest_framing` 加回节点表面。

## 验证证据

- 当前 ComfyUI Python：146 项单元/契约测试通过。
- Node 测试：7 项通过（4 项时间轴、3 项图片提及）。
- `web/h3lv.js`、`web/materials.js`、`web/material_mentions.js`、`web/expansion.js`：`node --check` 通过；全部 Python 文件 `py_compile` 通过。
- `controller.py`、`nodes.py`、扩写修复及相关测试已同步到安装目录，SHA-256 与仓库一致；安装副本重新运行 141 项 Python 测试、7 项 Node 测试、四份前端语法检查和全部 Python `compileall`，均通过。
- 开发测试工作流共 13 个输出，`segment_prompt` 未连接，`fps` 已连接 VHS `frame_rate`；默认扩写连线仍指向 H3 `prompt`。
- 本次用真实失败缓存 `af9218bb6f9351ad94497cc76a1f5437fe6b527232b1ab080fa47c60ed72c18f.invalid.txt` 验证了缺失标签自动修复；修复后仅有一个 `[Shot 1]` 且位于 `detailed_description` 开头。
- 本轮项目删除管理、保存/生成校验和 ASR 幻觉隔离涉及的 11 个文件已同步到安装目录，SHA-256 与仓库一致；安装目录重新运行 135 项 Python 测试、7 项 Node 测试、四份前端语法检查和全部 Python `compileall`，均通过。按要求没有替用户重启 ComfyUI。
- 尚未在重启后的真实浏览器中人工点击验收本轮的折叠显示、分段独立状态与自动迁移。

## 下一轮先做什么

1. 让用户手动启动 ComfyUI 并刷新页面。
2. 打开开发测试工作流，确认长视频节点末尾出现未连接的 `segment_prompt` 和已连接 VHS `frame_rate` 的 `fps`，原 `H3LVPromptExpand → PreviewAny → H3 prompt` 连线不变。
3. 新分析一段含长前奏的歌曲，确认所有被前奏覆盖的分段默认显示“人物氛围表演”，试听后再判断声学建议是否准确。
4. 确认导演简报六行字段可编辑；填写手写提示词、保存确认后，临时改接 `segment_prompt → H3 prompt`，核对文本原样到达 H3。测试后恢复默认扩写连线。
5. 验收项目默认参考图区默认收起且可反复展开，收起时分段素材控件仍立即可见，以及空白画布上传图片不再走 `ref_image` 接线报错。
6. 在默认和本段自定义素材说明中分别输入 `@`，确认缩略图选择、重排自动改号和引用中的图片禁止删除；再打开分镜提示词生成器，确认空素材说明的分段可生成，第二次相同输入显示“已读取匹配缓存”。
7. 未经用户明确要求不要推送 GitHub。
8. 用户重启 ComfyUI 后重试口播项目的提示词扩写，确认模型漏写 `[Shot 1]` 时节点能自动修复；真实 API 是否仍会抄录背景文字需人工检查新结果。

## 新对话可直接粘贴的提示词

```text
继续开发 MiniMax H3 长视频 ComfyUI 插件。先阅读：
D:\Codex\comfyui工作流\MiniMaxH3-LongVideo\development\ComfyUI-H3-LongVideo\HANDOFF.md

仓库位于：
D:\Codex\comfyui工作流\MiniMaxH3-LongVideo\development\ComfyUI-H3-LongVideo

当前分支是 feature/segment-materials-expander，安装目录和测试工作流路径都写在 HANDOFF.md。先检查 git 状态和最近提交，不要回退已有改动。ComfyUI 是否重启由我来操作；修改后同步插件文件但不要替我重启。先根据我接下来的截图和反馈继续调整，完成后运行 HANDOFF.md 中列出的测试。未经我明确要求不要推送 GitHub。
```
